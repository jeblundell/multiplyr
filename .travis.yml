language: r
sudo: true
cache: packages
branches:
    only:
        - master
        - stable
        - develop
r:
    - oldrel
    - release
    - devel
os:
    - linux
r_check_args: --as-cran --run-donttest --run-dontrun
after_success:
  - bash <(curl -s https://codecov.io/bash)
