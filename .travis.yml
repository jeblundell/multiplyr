language: r
sudo: false
cache: packages
branches:
    only:
        - master
        - stable
        - develop
r:
    - oldrel
    - release
    - devel
os:
    - linux
    - osx
before_install:
    - tlmgr install index
